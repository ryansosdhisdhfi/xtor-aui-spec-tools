# 与 Aidoc 工具链 venv 对齐的最小集，例如:
#   pip install -r /path/to/xtor-aui-spec-tools/requirements.txt
#
requests>=2.28.0
pyyaml>=6.0
pypdf>=4.0
docling>=2.0.0
docling-core>=2.0.0
pymupdf>=1.24.0
# 图内 OCR（可选 B4，且需本机 Tesseract + 语言包）
pytesseract>=0.3.10
Pillow>=10.0.0
